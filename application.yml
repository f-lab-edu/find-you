spring:
  datasource:
    url: jdbc:mysql://localhost:3306/findyou?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: aksrua
    password: 920214
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true